gigaam==0.1.0
# onnxruntime 1.17.3 from gigaam 0.1.0 is built against the NumPy 1.x ABI.
numpy<2
# MLX acceleration is installed only on Apple Silicon; Linux/WSL skips it.
mlx==0.31.1; sys_platform == "darwin" and platform_machine == "arm64"
